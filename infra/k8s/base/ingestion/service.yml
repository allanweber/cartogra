apiVersion: v1
kind: Service
metadata:
  name: ingestion
  labels:
    app: ingestion
    team: platform
spec:
  type: ClusterIP
  selector:
    app: ingestion
  ports:
    - name: http
      port: 8085
      targetPort: http
