apiVersion: v1
kind: Service
metadata:
  name: registry
  labels:
    app: registry
    team: platform
spec:
  type: ClusterIP
  selector:
    app: registry
  ports:
    - name: http
      port: 8081
      targetPort: http
