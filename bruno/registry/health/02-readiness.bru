meta {
  name: Readiness
  type: http
  seq: 2
}

get {
  url: {{mgmtUrl}}/actuator/health/readiness
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
