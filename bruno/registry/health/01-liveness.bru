meta {
  name: Liveness
  type: http
  seq: 1
}

get {
  url: {{mgmtUrl}}/actuator/health/liveness
  body: none
  auth: none
}

tests {
  test("status is 200", function() {
    expect(res.status).to.equal(200);
  });
}
