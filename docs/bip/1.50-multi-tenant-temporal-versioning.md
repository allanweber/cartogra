---
title: "Multi-Tenancy and History Tables: The Data Model Decisions That Age Well"
task: "1.50"
channels: [blog, twitter, instagram, linkedin, video]
status: draft
---

## Blog Post (600–1000 words)

**Title:** Multi-Tenancy and History Tables: The Data Model Decisions That Age Well

When you're building a SaaS product, two decisions you make in the first month will echo across
every feature you build for the next two years: how you isolate tenant data, and how you record
the history of changes. Get both wrong and you'll be rewriting your schema in production under
live traffic. Get them right and they become invisible infrastructure that makes everything else
easier.

Here's how Cartogra approaches both.

**Multi-tenancy: isolation by column, enforced by policy**

Every domain table in Cartogra has `tenant_id UUID NOT NULL`. Not most tables — every table, no
exceptions. This is enforced as a lint rule in the `check-constraints` slash command and verified
during code review.

Queries filter by `tenant_id` at the repository layer. But column-level filtering alone can be
defeated by a bug — a join that forgets the `WHERE tenant_id = :tenantId` clause, for example.
PostgreSQL's Row Level Security (RLS) adds a second enforcement layer:

```sql
ALTER TABLE services ENABLE ROW LEVEL SECURITY;
CREATE POLICY tenant_isolation ON services
    USING (tenant_id = current_setting('app.current_tenant_id')::UUID);
```

The RLS policy is a safety net, not the primary mechanism. The primary mechanism is the
application-level filter. RLS catches bugs that slip past review.

**How tenant identity flows through the system**

The JWT issued by the gateway carries a `tid` claim (tenant ID). The gateway's
`TenantInjectionGlobalFilter` strips inbound `X-Tenant-Id` headers from clients (a client
cannot forge their tenant context) and re-injects the value from the validated token. Downstream
services trust the injected header — they never decode a token.

Redis keys are prefixed: `tenant:{tenantId}:rate-limit:...`. No cross-tenant cache collision is
possible if this prefix is applied consistently. A project-wide Grep for Redis key construction
is part of the Phase 1 audit checklist.

**Temporal versioning: the history table pattern**

Soft deletes (`deleted_at TIMESTAMPTZ`) are table stakes — hard deletes make debugging and audit
impossible. But soft deletes only tell you that a record was deleted, not what it looked like
before. For a service registry, that's often not enough.

Cartogra adds a `service_history` table for point-in-time queries:

```sql
CREATE TABLE service_history (
    id            UUID        NOT NULL DEFAULT gen_random_uuid() PRIMARY KEY,
    service_id    UUID        NOT NULL,
    tenant_id     UUID        NOT NULL,
    changed_at    TIMESTAMPTZ NOT NULL DEFAULT now(),
    changed_by    UUID,
    change_type   TEXT        NOT NULL, -- CREATED | UPDATED | DELETED
    snapshot      JSONB       NOT NULL
);
```

On every mutation (create, update, soft-delete), the use case writes a new history row with a
JSONB snapshot of the service state at that moment. The history table is append-only — it is
never updated or soft-deleted. It's a log, not a mutable record.

This enables queries like: "What did `payment-service` look like six months ago?" — answered by
selecting the most recent history row before the target timestamp.

**Ownership history**

Team ownership changes over time. The `team_id` foreign key on `services` captures the current
owner. But questions like "who owned this service during the incident in November?" require a
history. The same pattern applies: `service_ownership_history` records every team assignment
change with a `changed_at` timestamp and the IDs of the previous and new team.

This matters for incident retrospectives, compliance audits, and understanding architectural
evolution. "When did the platform team take over the auth service?" is a query, not a memory
exercise.

**The JSONB snapshot trade-off**

Storing a full JSONB snapshot per change avoids schema coupling between the history table and
the current service record. When the service table gains a new column, old history rows still
contain valid snapshots of the previous shape. Migrations don't break history.

The cost: JSONB snapshots are larger than column-level diffs. For a service catalog at realistic
scale (hundreds of services, changes measured in dozens per day), this is not a performance
concern. The history table has a GIN index on `snapshot` for JSONB queries and a composite
index on `(service_id, changed_at)` for point-in-time lookups.

**Why these decisions age well**

Adding `tenant_id` to a table after launch requires a migration that backfills every existing
row. In a live multi-tenant database, that's a table lock or a shadow-table migration — painful,
risky, and expensive. Adding it first costs one column per table and zero production risk.

Similarly, adding a history table after engineers start asking "what did this look like last
month?" requires backfilling from external sources (logs, backups, memory). Adding it on day one
means those questions are always answerable.

Both decisions look like overhead until the day they're load-bearing.

---

## Twitter/X Thread (4–8 tweets)

**Tweet 1 (hook):**
Two data model decisions you make in month one echo across every feature for two years:

1. How you isolate tenant data
2. How you record change history

Get them wrong: painful schema migrations under live traffic.
Here's how Cartogra gets them right 🧵

**Tweet 2:**
Multi-tenancy: `tenant_id UUID NOT NULL` on every domain table. No exceptions.

Application filters at the repository layer. RLS policy as a safety net:

```sql
CREATE POLICY tenant_isolation ON services
  USING (tenant_id = current_setting('app.current_tenant_id')::UUID);
```

Two layers. Bugs in one don't leak tenant data.

**Tweet 3:**
Tenant identity flows from the JWT `tid` claim through the gateway's filter — injected as `X-Tenant-Id`. Downstream services trust the header. They never decode a token.

Redis keys: `tenant:{tenantId}:...` — cross-tenant cache collision is structurally impossible.

**Tweet 4:**
Soft deletes are table stakes. But "deleted at 14:32" doesn't tell you what the record looked like.

For a service registry, that's not enough.

**Tweet 5:**
Solution: a `service_history` table. On every mutation, write a JSONB snapshot:

```sql
CREATE TABLE service_history (
  service_id UUID, tenant_id UUID,
  changed_at TIMESTAMPTZ, change_type TEXT,
  snapshot JSONB
);
```

Append-only. Never updated. Point-in-time queries answered with one SELECT.

**Tweet 6:**
JSONB snapshot avoids schema coupling. When the service table gains a new column, old history rows still contain valid snapshots of the previous shape. Migrations don't break history.

**Tweet 7:**
Ownership history follows the same pattern. `service_ownership_history` records every team assignment change with timestamps.

"Who owned the auth service during the November incident?" — that's a query, not a memory exercise.

**Tweet 8:**
Both decisions look like overhead on day one.

They become load-bearing the first time someone asks "what did this look like six months ago?" and you have an answer instead of a shrug.

Building Cartogra in public. Repo below. 👇

---

## Instagram Carousel (5–8 slides)

**Slide 1 (hook):**
Two decisions you make in month one echo for years:

1. Tenant data isolation
2. Change history

Here's how to get both right ↓

**Slide 2:**
Multi-tenancy rule: `tenant_id UUID NOT NULL` on every table.

Two enforcement layers:
→ Application filter at repository layer
→ PostgreSQL RLS policy as safety net

Bugs in one can't leak cross-tenant data.

**Slide 3:**
Tenant identity flows from the JWT `tid` claim.

Gateway strips inbound `X-Tenant-Id` from clients.
Re-injects from the validated token.

Downstream services trust the header. Never decode a token.

**Slide 4:**
Redis keys: `tenant:{tenantId}:...`

Prefix every key. Cross-tenant cache collision is structurally impossible.

**Slide 5:**
Soft deletes tell you when. Not what.

For "what did this service look like 6 months ago?" — you need a history table.

**Slide 6:**
service_history: append-only JSONB snapshots.

Every mutation writes a row.
Point-in-time query = one SELECT.
New columns don't break old snapshots.

**Slide 7:**
Ownership history too.

Who owned the auth service during last November's incident?

That's a query. Not a memory exercise.

**Slide 8 (CTA):**
These decisions look like overhead on day one.

They're load-bearing the first time someone asks "what did this look like 6 months ago?"

Follow for more build-in-public architecture decisions.

**Caption (150–200 words):**
Two data model decisions determine how gracefully your SaaS ages: how you isolate tenant data and
how you record change history. Cartogra puts `tenant_id UUID NOT NULL` on every domain table and
enforces isolation at two layers — application-level repository filters plus PostgreSQL RLS
policies. Tenant identity flows from the gateway JWT `tid` claim and is injected as a header,
never decoded by downstream services. For history, every mutation writes an append-only JSONB
snapshot to a `service_history` table, enabling point-in-time queries and audit trails without
schema coupling. Ownership changes are tracked the same way. Both decisions look like overhead
in month one and become load-bearing infrastructure in month six. Building Cartogra in public.
#buildinpublic #SaaS #multitenancy #PostgreSQL #datamodel #softwaredevelopment
#platformengineering #microservices

---

## LinkedIn Post (300–500 words)

**Title:** The two data model decisions every SaaS should make on day one

After advising several engineering teams on SaaS data architecture, I've noticed the same two
patterns that consistently produce painful retrofits: adding multi-tenant isolation after the first
customer signs on, and adding change history after the first "what did this look like last month?"
support request.

Both are avoidable. Both decisions are straightforward to make correctly on day one and very
expensive to correct later.

**Multi-tenant isolation**

Cartogra places `tenant_id UUID NOT NULL` on every domain table — not most tables, every table.
At the application layer, all repository queries filter by tenant. As a safety net, PostgreSQL Row
Level Security policies enforce the same constraint at the database layer. A bug in the application
filter can't silently leak data across tenant boundaries because the database rejects the query
before it returns rows.

Tenant identity flows from the JWT `tid` claim through the API gateway's filter, injected as a
header that downstream services trust. Clients cannot forge their tenant context — the gateway
strips inbound tenant headers and replaces them with the value from the validated token.

Retrofitting this after launch means a migration that backfills `tenant_id` across every
production table while the system is live. That's a shadow table migration, careful ordering,
and significant risk. Doing it first costs one column per table.

**Temporal versioning**

Soft deletes (`deleted_at TIMESTAMPTZ`) are table stakes for any production system. But a
deletion timestamp doesn't answer "what did service X look like six months ago?" or "who
owned the auth service during November's incident?"

Cartogra writes an append-only JSONB snapshot to a `service_history` table on every mutation.
The snapshot contains the full service state at that moment. History rows are never updated or
deleted. Point-in-time queries select the most recent history row before the target timestamp
— one indexed query.

JSONB snapshots avoid schema coupling: when the service table gains a new column, existing
history rows remain valid representations of the previous data shape. Migrations don't break
the historical record.

Both decisions look like premature infrastructure when you're building for your first users.
They look like essential hygiene when you're having your first compliance conversation or
your first incident retrospective.

For teams building SaaS products: are your tenant isolation and history patterns something
you designed intentionally, or something that grew organically and now constrains what you
can build?

---

## Video Outline (optional)

**Title:** Multi-tenant isolation and change history — the data model decisions that pay off later

**Intro (0:00–0:30)**
- "Two schema decisions you make early become load-bearing infrastructure. Here's what they are."

**Section 1 — Multi-tenancy (0:30–3:00)**
- Show `services` table with `tenant_id` column
- Show RLS policy creation and test in psql
- Show gateway filter stripping + re-injecting the tenant header

**Section 2 — History table (3:00–5:30)**
- Show `service_history` table schema
- Live mutation → history row written
- Point-in-time query: "What did this service look like on date X?"

**Section 3 — Ownership history (5:30–7:00)**
- Show `service_ownership_history`
- Query: "Who owned auth-service in November?"

**Outro (7:00–7:30)**
- "Overhead on day one. Load-bearing infrastructure in month six."
- Link to blog post and repo.
