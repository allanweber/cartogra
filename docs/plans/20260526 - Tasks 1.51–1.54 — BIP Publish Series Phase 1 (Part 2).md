# Plan: Tasks 1.51–1.54 — BIP Publish Series: Phase 1 Enterprise-SCM, Demo Screenshots, Auth, and Rate-Limiting

## Feature Summary

Tasks 1.51–1.54 are the second wave of Phase 1 Build-in-Public publishing items. Together they cover
the enterprise-SCM multi-provider story (1.51), a visual product demo with OpenAPI links (1.52), the
dual-mode auth deep-dive (1.53), and the Redis rate-limiting deep-dive (1.54). All four are
documentation-only tasks — no code, no schema changes. For 1.53 and 1.54, full drafts already exist
in `docs/bip/` under the wrong task IDs (`1.58` and `1.59`); the plan task for those items is to
create correctly named files with proper frontmatter referencing the correct task IDs. Tasks 1.51 and
1.52 require new drafts to be written from scratch. The output of this plan is four draft BIP files
committed to `docs/bip/`.6

---

## GitHub Workflow

### Issues

**1.51 — new issue required:**
- Title: `US1.51 — BIP: enterprise-SCM multi-provider angle`
- Acceptance criteria:
  - `docs/bip/1.51-enterprise-scm-multi-provider.md` exists with all five channel drafts at `status: draft`
  - Blog post covers the ScmProvider SPI, both GitHub and Azure DevOps provider implementations, CODEOWNERS ownership mapping, and why multi-provider matters for enterprise teams
  - Twitter thread hooks on the "one registry, two SCM providers" angle
  - Instagram carousel explains the multi-provider abstraction visually
  - LinkedIn post (300–500 words) frames this for platform and engineering leadership, ends with an engagement question
- Milestone: `Phase 1 — Registry & Gateway`

**1.52 — new issue required:**
- Title: `US1.52 — BIP: catalog UI screenshots and OpenAPI links`
- Acceptance criteria:
  - `docs/bip/1.52-catalog-ui-screenshots-openapi.md` exists with all five channel drafts at `status: draft`
  - Blog post describes the catalog list view, service detail, filters, and links to `docs/api/gateway.openapi.yaml` and `docs/api/registry.openapi.yaml` for the full API surface
  - Twitter thread uses screenshot descriptions as hooks; final tweet links to repo and OpenAPI docs
  - Instagram carousel (5–8 slides) describes each screen section for a designer to recreate as screenshots
  - LinkedIn post positions the demo as "here is what the product actually looks like"
- Milestone: `Phase 1 — Registry & Gateway`

**1.53 — new issue required:**
- Title: `US1.53 — BIP: dual-mode auth article (httpOnly cookies + Bearer tokens)`
- Acceptance criteria:
  - `docs/bip/1.53-dual-mode-auth-gateway.md` exists with all five channel drafts at `status: draft`
  - Content may be taken from the mislabelled `docs/bip/1.58-dual-mode-auth-gateway.md` with correct frontmatter applied
  - Blog post covers the per-service-auth trap, gateway-only validation, httpOnly cookie vs Bearer dual transport, tenant header injection, and the reactive + virtual threads combination
  - All five channels are populated
- Milestone: `Phase 1 — Registry & Gateway`

**1.54 — new issue required:**
- Title: `US1.54 — BIP: Redis rate-limiting article (token buckets, per-tenant key isolation, 429 behavior)`
- Acceptance criteria:
  - `docs/bip/1.54-redis-rate-limiting.md` exists with all five channel drafts at `status: draft`
  - Content may be taken from the mislabelled `docs/bip/1.59-redis-rate-limiting.md` with correct frontmatter applied
  - Blog post covers credential stuffing, token bucket algorithm, auth-endpoint stricter config, observable 429 envelope, Testcontainers Redis integration test, and the per-tenant key isolation Phase 3 preview
  - All five channels are populated
- Milestone: `Phase 1 — Registry & Gateway`

### Branch

```
feat/1.51-1.54-bip-publish-series
```

All four BIP items are content-only. No code or schema changes; a single branch covers them all.

### PR

- Title: `feat: BIP publish series — enterprise SCM, demo screenshots, auth, rate-limiting (1.51–1.54)`
- Body must include:
  - `Closes #<1.51 issue>`
  - `Closes #<1.52 issue>`
  - `Closes #<1.53 issue>`
  - `Closes #<1.54 issue>`
  - The full **E2E test strategy** (below)
  - The full **Acceptance Criteria** list (below)
- Milestone: `Phase 1 — Registry & Gateway`
- CI must be green (build + tests) before requesting review.

#### E2E Test Strategy (PR body)

**Prerequisites:** Repo checked out on `feat/1.51-1.54-bip-publish-series`.

**Steps:**

1. Confirm all four BIP files exist:
   ```
   ls docs/bip/1.51-enterprise-scm-multi-provider.md
   ls docs/bip/1.52-catalog-ui-screenshots-openapi.md
   ls docs/bip/1.53-dual-mode-auth-gateway.md
   ls docs/bip/1.54-redis-rate-limiting.md
   ```
2. Open each file and verify frontmatter contains `status: draft`, the correct `task:` value, and
   `channels: [blog, twitter, instagram, linkedin, video]`.
3. Verify each blog post section is 600–1000 words (rough count is fine).
4. Verify each Twitter thread has 4–8 numbered tweets and tweet 1 is a standalone hook.
5. Verify each Instagram carousel has 5–8 named slides with Slide 1 as the hook and the last slide
   as a CTA.
6. Verify each LinkedIn post is 300–500 words and ends with an engagement question.
7. For 1.53 and 1.54: confirm the old mislabelled files (`1.58-dual-mode-auth-gateway.md`,
   `1.59-redis-rate-limiting.md`) are either removed or left as-is with a note; confirm the correct
   files exist with correct frontmatter.
8. Run `./gradlew build` from repo root — content files must not break the build.

**Passing signal:** All four files present; all sections populated; no TODO placeholders remaining;
frontmatter task IDs correct; build green.

**Failure triage:**
- Missing file: check the branch is up to date and the file was committed.
- Incomplete section: a section with only a placeholder is not a draft — complete it.
- Wrong task ID in frontmatter: edit the `task:` field to match the checklist ID.

#### Acceptance Criteria (PR body)

- [ ] AC-1: `docs/bip/1.51-enterprise-scm-multi-provider.md` exists with `status: draft` and `task: "1.51"`
- [ ] AC-2: 1.51 blog post is 600–1000 words and covers both GitHub and Azure DevOps providers
- [ ] AC-3: 1.51 Twitter thread has 4–8 tweets; tweet 1 hooks on "one registry, two SCM providers"
- [ ] AC-4: 1.51 Instagram carousel has 5–8 slides with hook and CTA
- [ ] AC-5: 1.51 LinkedIn post is 300–500 words, professionally framed, ends with engagement question
- [ ] AC-6: `docs/bip/1.52-catalog-ui-screenshots-openapi.md` exists with `status: draft` and `task: "1.52"`
- [ ] AC-7: 1.52 blog post describes catalog list, detail, filters, and links to OpenAPI YAML files
- [ ] AC-8: 1.52 Twitter thread and Instagram carousel describe the UI screens
- [ ] AC-9: 1.52 LinkedIn post frames the demo professionally
- [ ] AC-10: `docs/bip/1.53-dual-mode-auth-gateway.md` exists with `status: draft` and `task: "1.53"`
- [ ] AC-11: 1.53 content covers gateway-only auth, httpOnly cookie, Bearer, tenant injection
- [ ] AC-12: All five channels populated in 1.53
- [ ] AC-13: `docs/bip/1.54-redis-rate-limiting.md` exists with `status: draft` and `task: "1.54"`
- [ ] AC-14: 1.54 content covers token bucket, auth endpoint stricter config, observable 429, Testcontainers Redis test
- [ ] AC-15: All five channels populated in 1.54
- [ ] AC-16: `docs/execution-checklist.md` tasks 1.51–1.54 are marked `[x]`

### Commit and push approval

Per CLAUDE.md: never commit or push without Allan's explicit approval. Show diff summary + draft
message and ask "OK to commit?" before every commit.

---

## Dependencies & Sequencing

- **Requires:** Tasks 1.28–1.37 (both SCM providers coded and tested), 1.42 (catalog UI crafted),
  1.21–1.22 (JWT issuance stable end-to-end), 1.25 (rate limiting tested and observable) — all
  confirmed complete.
- **Requires for 1.53/1.54:** Draft content already present in mislabelled BIP files
  (`docs/bip/1.58-dual-mode-auth-gateway.md`, `docs/bip/1.59-redis-rate-limiting.md`); no new
  content authoring required, only correct file creation.
- **Order within plan:** All four tasks are independent content files; any order works.
- **Unblocks:** No code tasks — these are terminal BIP items for Phase 1.

---

## Per-Task Implementation

### Task 1.51 — Enterprise-SCM Multi-Provider Angle (LinkedIn-first)

**What to build:** A five-channel BIP draft emphasising the enterprise-SCM integration story: both
GitHub and Azure DevOps are supported through a single `ScmProvider` SPI; CODEOWNERS ownership
mapping works across both; enterprise teams with mixed SCM environments get one registry instead of
two.

**Files to create or modify:**

| File | Action | Notes |
|------|--------|-------|
| `docs/bip/1.51-enterprise-scm-multi-provider.md` | New | Full five-channel draft |
| `docs/execution-checklist.md` | Modify | Mark 1.51 `[x]` after file exists |

**Critical constraints:**
- LinkedIn post is the primary channel for this topic; it must be 300–500 words, professionally
  framed for platform engineers and tech leads, and end with an engagement question.
- All other channels must still be present — LinkedIn-first does not mean LinkedIn-only.
- Do not duplicate content verbatim from `1.36-scm-spi-adr.md` or `1.37-provider-comparison.md`;
  the enterprise-SCM angle here is about the multi-provider registry outcome (catalog shows both),
  not the SPI architecture itself.

---

### Task 1.52 — Catalog UI Screenshots and OpenAPI Links

**What to build:** A five-channel BIP draft showing what Cartogra looks like in the browser — the
catalog list with filters, the service detail page, the SCM provider badge, and health indicators —
and pointing to the OpenAPI specs in `docs/api/`.

**Files to create or modify:**

| File | Action | Notes |
|------|--------|-------|
| `docs/bip/1.52-catalog-ui-screenshots-openapi.md` | New | Full five-channel draft |
| `docs/execution-checklist.md` | Modify | Mark 1.52 `[x]` after file exists |

**Critical constraints:**
- Instagram carousel slide descriptions must be detailed enough for a designer to produce
  screenshots from them (describe layout, labels, badge positions, filter state).
- Blog post must include direct links (relative repo paths) to `docs/api/gateway.openapi.yaml`
  and `docs/api/registry.openapi.yaml` — these are the published API surface.
- Do not claim screenshots are live screenshots if they are screen descriptions; use language like
  "the catalog list page shows…" rather than "screenshot: …".

---

### Tasks 1.53 and 1.54 — Correctly-Named Files from Existing Mislabelled Drafts

**What to build:** Two new BIP files with correct task IDs, sourcing content from the existing
mislabelled drafts.

**Context:** At some point during planning, two BIP drafts were written with task IDs `1.58` and
`1.59` instead of the correct `1.53` and `1.54`. The content is complete and correct; only the
file names and frontmatter `task:` fields are wrong.

**Files to create or modify:**

| File | Action | Notes |
|------|--------|-------|
| `docs/bip/1.53-dual-mode-auth-gateway.md` | New | Copy content from `1.58-dual-mode-auth-gateway.md`; update `task: "1.53"` in frontmatter |
| `docs/bip/1.54-redis-rate-limiting.md` | New | Copy content from `1.59-redis-rate-limiting.md`; update `task: "1.54"` in frontmatter |
| `docs/bip/1.58-dual-mode-auth-gateway.md` | Retain | Leave in place; the content is still valid reference material |
| `docs/bip/1.59-redis-rate-limiting.md` | Retain | Leave in place; the content is still valid reference material |
| `docs/execution-checklist.md` | Modify | Mark 1.53 and 1.54 `[x]` |

**Critical constraints:**
- The only changes needed to the content from the mislabelled files are the frontmatter `task:`
  field: `task: "1.53"` and `task: "1.54"` respectively.
- Do not alter the body content — it is complete and accurate.

---

## Schema Changes

N/A — no new database tables or migrations.

---

## Env Vars Delta

N/A — no new configuration.

---

## Test Strategy

**Unit tests (no containers):** N/A — content files, not code.

**Integration tests (Testcontainers):** N/A — content files, not code.

**What NOT to test here:** Actual publishing to external platforms — that happens manually after
the draft is approved. Platform-specific formatting (Twitter character count, LinkedIn formatting)
is verified by human review, not automation.

---

## New Error Codes

N/A — no new error codes required.

---

## Postman Collection

N/A — no HTTP surface. These tasks produce documentation files only.

---

## Documentation

### PlantUML Diagrams

N/A — BIP content files do not require PlantUML diagrams. The architecture they describe is
already diagrammed in earlier tasks.

### ADR

N/A — the decisions referenced in these BIP articles are already captured in ADR-0010
(gateway-only auth), ADR-0011 (dual-mode token delivery), ADR-0013 (SCM SPI), and ADR-0014
(sync idempotency).

### OpenAPI

N/A — no HTTP endpoint changes.

---

## Rollback Plan

Revert the branch — no persistent state change. Content files only.

---

## Verification Script

1. `ls docs/bip/1.51-enterprise-scm-multi-provider.md` — file exists
2. `ls docs/bip/1.52-catalog-ui-screenshots-openapi.md` — file exists
3. `ls docs/bip/1.53-dual-mode-auth-gateway.md` — file exists
4. `ls docs/bip/1.54-redis-rate-limiting.md` — file exists
5. Open each file; confirm frontmatter has the correct `task:` value, `status: draft`, and all
   five channel headings present with no section left as an unfilled placeholder.
6. For 1.51: verify the LinkedIn section is 300–500 words and ends with a question.
7. For 1.52: verify the blog post contains relative links to `docs/api/gateway.openapi.yaml` and
   `docs/api/registry.openapi.yaml`.
8. `./gradlew build` from repo root — confirm still green.

---

## BIP

### Task 1.51 — Enterprise-SCM Multi-Provider Angle

**Draft file:** `docs/bip/1.51-enterprise-scm-multi-provider.md`

---

#### Blog post (600–1000 words)

**Title:** One Registry for GitHub Teams and Azure DevOps Teams — No Compromises

Enterprise engineering organisations rarely use one SCM. Acquisitions bring different toolchains.
Separate business units standardised on different providers. A founding team that preferred GitHub
acquired by a company standardised on Azure DevOps. The result: two SCM environments, two siloed
service registries, and an ops team that owns services in both without a unified view.

Cartogra's answer is a `ScmProvider` SPI that makes the registry provider-agnostic by design.

**The provider problem**

A service registry that only talks to GitHub is not an enterprise-grade catalog. The GitHub
provider knows how to list repos, fetch `service.yaml`, read `CODEOWNERS`, and register org-level
webhooks. The Azure DevOps provider knows how to list repos across a project collection, read the
same files from a different path hierarchy, and register Azure DevOps service hooks. Their APIs,
authentication models, and webhook semantics are completely different.

The naive approach — branching on provider type inside the sync logic — creates a maintenance
burden that grows with every provider added. Change the sync algorithm once, test it for both
providers, ship it once.

**The ScmProvider SPI**

Cartogra's ingestion service defines a Spring-free interface:

```java
public interface ScmProvider {
    String providerType();
    List<ScmRepository> listRepositories(ScmConnectionConfig config);
    Optional<String> getFileContents(ScmConnectionConfig config, String repo, String path);
    OwnershipMap resolveOwnership(ScmConnectionConfig config, ScmRepository repository);
    void registerWebhook(ScmConnectionConfig config, String targetUrl);
    void deregisterWebhook(ScmConnectionConfig config, String externalWebhookId);
    boolean verifyWebhookSignature(ServerHttpRequest request, byte[] rawBody, ScmConnectionConfig config);
    boolean isRelevantWebhookEvent(ServerHttpRequest request);
}
```

`GitHubProvider` implements it using GitHub's REST API with PAT or GitHub App credentials.
`AzureDevOpsProvider` implements it using Azure DevOps REST APIs with PAT or service-principal
credentials. The sync use case dispatches by `providerType` string — it contains no
provider-specific code.

Credentials are not environment variables. They live in the `scm_connections` table as JSONB,
scoped per tenant per connection. A single Cartogra tenant can have five GitHub connections for
five different organisations and two Azure DevOps connections for two business units — all
syncing in parallel, all with their own credentials and webhook registrations.

**What shows up in the catalog**

Once both providers are synced, the catalog list page renders services from both SCM sources
side by side. The SCM provider badge on each service card shows the source (GitHub mark or Azure
DevOps mark). The detail page shows which connection the service was discovered from, when it was
last synced, and the current sync health.

CODEOWNERS ownership mapping works across both providers. The ownership resolution algorithm is
identical — it reads the parsed `OwnershipMap` from whichever provider produced it and applies
the ADR-0015 matching rule (single unambiguous team match → set `services.team_id`). The sync
use case doesn't need to know whether the CODEOWNERS came from GitHub or Azure DevOps.

**Why this matters for enterprise teams**

A registry that forces you to pick one SCM provider creates a catalog blind spot for everything
on the other provider. Worse, it incentivises teams to maintain parallel registries — one per
provider — which is the problem a registry is supposed to solve.

With provider-agnostic sync, the catalog reflects the complete service estate. SREs looking at
blast radius analysis see dependencies across both provider-sourced services. Tech leads doing
architecture reviews see the full picture. The question "which services don't have a registered
owner?" is answerable across the entire tenant, not just the GitHub half of it.

Adding GitLab, Bitbucket, or an internal Gitea instance in the future requires implementing one
interface. The sync pipeline, ownership resolution, webhook controller, and catalog API are
unchanged.

---

#### Twitter/X thread (5 tweets)

**Tweet 1 (hook):**
Most service registries pick a side: GitHub only, or Azure DevOps only.

Enterprise teams use both.

Here's how Cartogra syncs from both — with one codebase, one use case, and a provider SPI that
makes the difference invisible. 🧵

**Tweet 2:**
The ScmProvider interface:

```java
interface ScmProvider {
  List<ScmRepository> listRepositories(config);
  OwnershipMap resolveOwnership(config, repo);
  boolean verifyWebhookSignature(request, body, config);
}
```

GitHubProvider and AzureDevOpsProvider implement it.
The sync use case dispatches by `providerType` string.
No provider-specific logic in the core algorithm.

**Tweet 3:**
Credentials are per-connection in the DB, not env vars.

One tenant: 3 GitHub orgs + 2 Azure DevOps projects = 5 independent sync connections, all with
their own credentials and webhook registrations.

**Tweet 4:**
CODEOWNERS ownership mapping works identically across both providers.

The ownership resolution reads an `OwnershipMap` — it doesn't care if CODEOWNERS came from
GitHub or Azure DevOps. Adding a new provider means writing one class.

**Tweet 5:**
The catalog shows services from both sources side by side — SCM provider badge on every card.

One registry. Complete estate visibility. No "GitHub-only" blind spot.

Building Cartogra in public. Repo below. 👇

---

#### Instagram carousel (5 slides)

**Slide 1 — Hook:**
Visual: Split screen — GitHub logo + Azure DevOps logo pointing to one registry icon
Text: "Your services are in GitHub AND Azure DevOps. Your registry shouldn't pick a side."

**Slide 2 — The SPI:**
Visual: UML-style diagram with `ScmProvider` interface box at top, `GitHubProvider` and
`AzureDevOpsProvider` boxes below it, arrows labelled "implements"
Text: "One interface. Two providers. The sync use case doesn't know which one it's talking to."

**Slide 3 — Per-Connection Credentials:**
Visual: Diagram of one tenant with three GitHub connection nodes and two Azure DevOps nodes, each
with a small lock icon
Text: "Credentials are per-connection, not shared env vars. Add any org, any project."

**Slide 4 — Unified Catalog View:**
Visual: Mockup of service list showing GitHub badge on some cards, Azure DevOps badge on others,
interleaved naturally
Text: "One catalog. GitHub services and Azure DevOps services, side by side. Full estate visibility."

**Slide 5 — CTA:**
Visual: Cartogra logo + GitHub repo link
Text: "Adding GitLab = one new class. Follow for more Cartogra build-in-public updates."

Caption (150–200 words): Enterprise engineering teams rarely use just one SCM provider.
Acquisitions, organisational history, and team preferences create mixed GitHub + Azure DevOps
environments that naive service registries can't handle without picking a side. In Cartogra, the
`ScmProvider` SPI abstracts provider differences entirely — the sync algorithm, webhook controller,
and ownership resolution are all provider-agnostic. Credentials are stored per-connection in the
database, not as shared environment variables, so a single tenant can have multiple GitHub
organisations and Azure DevOps projects syncing in parallel with independent secrets and webhook
registrations. The catalog shows services from both sources with a provider badge on each card.
CODEOWNERS ownership mapping works identically across both. Adding GitLab or Bitbucket in a
future phase requires implementing one interface. Building Cartogra in public — a living service
registry with enterprise-grade multi-provider SCM support. Follow for updates.
#buildinpublic #platformengineering #GitHub #AzureDevOps #serviceregistry #microservices
#enterpriseengineering #devtools

---

#### LinkedIn post (300–500 words)

**Title:** Why Your Service Registry Should Be SCM-Agnostic — and How We Built That in Cartogra

Enterprise engineering organisations rarely standardise on a single source control provider.
Acquisitions bring different toolchains. Teams that grew organically made different choices.
A startup built on GitHub gets acquired by an organisation standardised on Azure DevOps. The
result is a mixed SCM estate — and most service registries are not built for it.

In Cartogra (a multi-tenant service registry platform we're building in public), we addressed
this with a `ScmProvider` interface in the ingestion service. `GitHubProvider` and
`AzureDevOpsProvider` implement it. The sync algorithm, webhook routing, and CODEOWNERS ownership
resolution are all provider-agnostic — they dispatch to the correct implementation by provider
type string and never contain provider-specific logic.

**What this means in practice**

Credentials are stored per-connection in the database as JSONB, not as global environment
variables. A tenant can have three GitHub organisation connections and two Azure DevOps project
connections active simultaneously, each with its own PAT or service-principal credentials and
webhook registration.

The catalog list renders services from all connections side by side, with a provider badge on
each card. CODEOWNERS ownership mapping applies the same resolution algorithm regardless of
provider. Blast-radius analysis and dependency graphs include services from all registered
sources — no "GitHub-only" blind spot.

**The extensibility outcome**

Adding GitLab, Bitbucket, or an internal Gitea instance in the future requires implementing the
`ScmProvider` interface — approximately 300 lines for a new provider. The sync pipeline, webhook
controller, ownership resolution, and catalog API are unchanged.

This design pays for itself the first time you inherit an acquisition's service estate. The new
organisation's Azure DevOps project becomes another connection in the same tenant. Their services
appear in the same catalog, owned by the same teams (or flagged as unmatched for manual
assignment), visible in the same dependency graph.

A registry with a provider lock-in creates a second catalog for the other half of the estate.
A registry designed around a provider SPI doesn't.

For engineering leaders managing mixed SCM environments: what's been the biggest barrier to
getting a unified view of your service estate? Provider support, ownership model, or something
else?

---

#### Video outline (optional)

> **Optional — do not block the checklist task on this.**

**Title:** GitHub + Azure DevOps in one registry — live sync demo

1. (0:00–0:45) Problem statement: mixed SCM estates in enterprise; what a registry "picking a
   side" costs you
2. (0:45–2:30) Code walkthrough: `ScmProvider` interface, `GitHubProvider` and
   `AzureDevOpsProvider` — the abstraction boundary
3. (2:30–4:00) Connection configuration: show adding a GitHub org connection and an Azure DevOps
   project connection in the Cartogra UI
4. (4:00–6:00) Live sync: trigger both connections; show the catalog populating with services
   from both providers; SCM badge visible on each card
5. (6:00–7:00) CODEOWNERS demo: show ownership resolving identically from both providers
6. (7:00–7:30) "Adding GitLab = one interface implementation. Repo below."

---

### Task 1.52 — Catalog UI Screenshots and OpenAPI Links

**Draft file:** `docs/bip/1.52-catalog-ui-screenshots-openapi.md`

---

#### Blog post (600–1000 words)

**Title:** Here's What Cartogra Actually Looks Like (and the Full API Docs)

Build-in-public posts about architecture decisions are useful. Sometimes the most useful thing is
to show what the product actually does.

This post is that post. Here's a walk through the Cartogra catalog UI and pointers to the full
API surface for engineers who want to integrate programmatically.

**The catalog list**

The catalog home page (`/catalog`) is a dense list of all registered services for the tenant. The
design principle is information density — no card-grid with large whitespace, no hero banners. Each
row in the list renders:

- Service name (linked to the detail page)
- Health badge: `healthy` (green), `degraded` (amber), or `down` (red)
- Owner team name
- SCM provider badge (GitHub or Azure DevOps icon)
- Language/stack tag
- Last synced timestamp

The filter bar at the top is persistent: filter by team, health status, tech stack, SCM provider,
or free-text search. Filters are applied client-side against the loaded service list for
responsiveness. The `orphan` filter surfaces services with no registered team owner — a leading
indicator of catalog drift.

The list is virtualized via TanStack Table for tenants with hundreds of services. Empty states
are handled: "No services matched your filters" differs from "No services registered yet."

**The service detail page**

Navigating to `/catalog/{serviceId}` renders the full service record. Key sections:

- **Overview**: name, description, health status, language, deployment tier, custom metadata
- **Ownership**: team name linked to the teams page; last CODEOWNERS sync timestamp
- **SCM connection**: which connection sourced this service, repo URL, last sync result
- **OpenAPI contract**: link to the associated contract version (Phase 3); stub in Phase 1
- **History**: last 10 material changes from `service_history`, with change type and actor

The detail page is read-only in Phase 1. Editing is a Phase 3 feature tied to contract management.

**The auth screens**

The login page (`/login`) renders an email/password form and OAuth provider buttons (Google and
GitHub). The register page (`/register`) captures name, email, and password and triggers OTP
verification. The verify-email page (`/verify-email`) is a single field accepting the 6-digit
code. All auth state is managed in httpOnly cookies — no token visible in `localStorage`.

Error states surface the `traceId` from the API error envelope in an amber alert banner. A user
who encounters an auth error can report the trace ID to support and we can look up the exact
request in the observability pipeline.

**The SCM Connections settings page**

`/settings/scm-connections` lists registered SCM connections with their sync status, last sync
timestamp, and a health badge. The add-connection drawer is a two-step form: select provider,
then enter connection name and PAT/credentials. Deleting a connection requires a confirmation
dialog and soft-deletes the record — a subsequent sync will transition affected services to
`orphaned` status.

**The full API surface**

The implemented API is documented in two OpenAPI specs committed to the repository:

- **Gateway API** (auth, proxying, rate limiting): [`docs/api/gateway.openapi.yaml`](../../docs/api/gateway.openapi.yaml)
- **Registry API** (services, teams, SCM connections, history): [`docs/api/registry.openapi.yaml`](../../docs/api/registry.openapi.yaml)

Both specs use the shared response envelope component defined in `gateway.openapi.yaml`. Every
endpoint documents the `data` / `traceId` success shape, the `error` / `traceId` error shape,
and the `X-Trace-Id` response header. The `X-Tenant-Id` header is injected by the gateway and
forwarded to registry — it appears as a required header on registry endpoints but is set
automatically by the gateway for authenticated browser sessions.

Forward-looking specs for topology, contract, and intelligence are maintained as explicit stubs
in `docs/api/` with Phase ownership markers indicating when each endpoint will be implemented.

**What's next**

The dependency graph, contract hub, and intelligence panel are the Phase 2–4 features on the
roadmap. The catalog list in Phase 1 is the foundation they build on: a registry that stays
accurate because it reads from SCM truth instead of waiting for engineers to update it.

---

#### Twitter/X thread (5 tweets)

**Tweet 1 (hook):**
Six months into building Cartogra, here's what the product actually looks like.

Catalog list, service detail, SCM connections, auth screens. Plus the full OpenAPI docs.

Thread 🧵

**Tweet 2:**
The catalog list: every service, with health badge (green/amber/red), owner team, SCM provider
badge, and last sync timestamp.

Filter bar: by team, health, tech stack, provider, or free text. Persistent. Virtualised.

Orphan filter: surfaces services with no registered owner.

**Tweet 3:**
The service detail page: overview, ownership (last CODEOWNERS sync), SCM connection source,
OpenAPI contract link (Phase 3), and the last 10 changes from the history table.

Append-only history. Point-in-time queries.

**Tweet 4:**
Auth screens: login with email/password or OAuth (Google, GitHub). httpOnly cookies — no token
in localStorage, no XSS risk. Error states surface the traceId for support escalation.

**Tweet 5:**
Full API docs:
→ Gateway (auth, rate limiting): `docs/api/gateway.openapi.yaml`
→ Registry (services, teams, SCM connections): `docs/api/registry.openapi.yaml`

Both use the shared response envelope. Every endpoint documents success + error shapes.

Building Cartogra in public. Repo below. 👇

---

#### Instagram carousel (6 slides)

**Slide 1 — Hook:**
Visual: Clean mock of a service catalog list — rows of services with green/amber health dots,
team badges, GitHub/AzDO icons, compact rows
Text: "Here's what a living service registry actually looks like."

**Slide 2 — Catalog List Features:**
Visual: Annotated diagram of a catalog list row with callouts pointing to: health badge (green
dot), owner team name, SCM provider icon, "orphan" warning badge on one row
Text: "Health. Owner. Source. Orphan detection. All visible at a glance."

**Slide 3 — Service Detail Page:**
Visual: Mock of a detail page with sections: Overview (name, tier, language), Ownership (team
name, last sync), History (3 rows: CREATED, UPDATED, UPDATED with timestamps)
Text: "Detail page: ownership, source, and a full change history — append-only."

**Slide 4 — Auth Screens:**
Visual: Login form mock — email/password fields, Google button, GitHub button; small amber alert
at bottom showing "Trace ID: 4bf92f..."
Text: "Login: OAuth or email/password. httpOnly cookie. Error states show the trace ID."

**Slide 5 — OpenAPI Docs:**
Visual: Partial screenshot-style render of the YAML frontmatter block from
`registry.openapi.yaml` showing the `ApiResponse` schema component
Text: "Full API docs in the repo. Every endpoint: success envelope + error envelope + X-Trace-Id."

**Slide 6 — CTA:**
Visual: Cartogra GitHub repo link + "API docs in docs/api/"
Text: "Open source. Phase 1 complete. Build-in-public continues. Follow for Phase 2."

Caption (150–200 words): After six months of build-in-public, here's what the Cartogra catalog
UI looks like in Phase 1. The catalog list shows every registered service with health status,
owner team, SCM provider badge, and last sync timestamp. A filter bar with orphan detection
surfaces services that have no registered owner — a leading indicator of catalog drift. The
service detail page shows the full history from an append-only history table: every material
change, timestamped, with actor and change type. Auth screens use httpOnly cookies for browser
sessions and Bearer tokens for API clients; error states surface trace IDs for support
escalation. The full API surface is documented in OpenAPI format in the repository at
`docs/api/gateway.openapi.yaml` and `docs/api/registry.openapi.yaml`. Phase 2 brings the
dependency graph; Phase 3 brings the contract hub. Follow for the build-in-public updates.
#buildinpublic #serviceregistry #platformengineering #microservices #OpenAPI #devtools
#productdesign #SpringBoot

---

#### LinkedIn post (300–500 words)

**Title:** Six Months of Building in Public — Here's What Cartogra Looks Like Today

I've been documenting the architectural decisions behind Cartogra for several months: the
gateway-only auth pattern, the SCM sync pipeline, the multi-tenant data model. Today I want to
show what the product actually looks like from a user perspective.

**The catalog list**

The catalog home page renders every registered service with health status, owner team, SCM
provider source, and last sync timestamp. A persistent filter bar supports filtering by team,
health status, tech stack, and provider — plus a free-text search and an orphan filter that
surfaces services with no registered team owner. Orphan detection is a leading indicator of
catalog drift: a service that nobody owns is a service that won't get updated when its team
changes.

**The service detail**

The detail page shows an overview (name, tier, language, custom metadata), the team owner with
the last CODEOWNERS sync timestamp, the SCM connection that sourced the service, and an
append-only change history from the `service_history` table. The history table records every
material change with timestamp and actor — answering "what did this service look like three
months ago?" with a single database query.

**The full API surface**

The implemented API is published as OpenAPI specs in the repository:

- [Gateway API](../../docs/api/gateway.openapi.yaml) — auth routes, token issuance, rate limiting,
  proxying behaviour
- [Registry API](../../docs/api/registry.openapi.yaml) — service CRUD, team ownership, SCM
  connections, history queries

Both use a shared response envelope that includes a `traceId` in every response — success and
error alike. The trace ID is the same value as the W3C `traceparent` distributed trace
identifier, so a user-reported error trace ID immediately locates the request across the entire
observability pipeline.

**What's coming next**

Phase 2 brings the dependency graph: blast-radius analysis, cycle detection, observed-vs-declared
dependency views powered by OpenTelemetry span data. Phase 3 brings the contract hub: OpenAPI
compatibility checks, breaking-change detection, and CI integration.

For engineering leaders evaluating service registry options: what feature would you need to see
in a catalog before trusting it for blast-radius analysis during an incident?

---

#### Video outline (optional)

> **Optional — do not block the checklist task on this.**

**Title:** Cartogra Phase 1 demo walkthrough — catalog, auth, and OpenAPI docs

1. (0:00–0:30) "Here's what six months of building in public looks like as a product."
2. (0:30–2:30) Catalog list walkthrough: health badges, owner teams, SCM badges, orphan filter
3. (2:30–4:30) Service detail page: overview, history table, ownership section
4. (4:30–5:30) Auth screens: login, OTP verify, OAuth flow, httpOnly cookie
5. (5:30–6:30) SCM Connections settings: add a GitHub connection, trigger a sync, show the
   catalog updating
6. (6:30–7:30) OpenAPI docs: open `gateway.openapi.yaml` and `registry.openapi.yaml` in editor;
   walk through the shared envelope component; show a real curl against the API
7. (7:30–8:00) "Phase 2 is the dependency graph. Follow for updates." Repo link.

---

### Tasks 1.53 and 1.54 — Content from Existing Mislabelled Draft Files

The full BIP content for tasks 1.53 and 1.54 already exists at:

- `docs/bip/1.58-dual-mode-auth-gateway.md` → copy to `docs/bip/1.53-dual-mode-auth-gateway.md`
- `docs/bip/1.59-redis-rate-limiting.md` → copy to `docs/bip/1.54-redis-rate-limiting.md`

The only change required in each file is the frontmatter `task:` field:

**For 1.53:**
```markdown
---
title: "Your Auth Lives in the Gateway — Not the Service"
task: "1.53"
channels: [blog, twitter, instagram, linkedin, video]
status: draft
---
```

**For 1.54:**
```markdown
---
title: "One Misconfigured Rate Limit Let Attackers Enumerate Usernames"
task: "1.54"
channels: [blog, twitter, instagram, linkedin, video]
status: draft
---
```

Refer to `docs/bip/1.58-dual-mode-auth-gateway.md` and `docs/bip/1.59-redis-rate-limiting.md`
for the complete body content of both files.

---

## Files Created / Modified

| File | Action | Task |
|------|--------|------|
| `docs/bip/1.51-enterprise-scm-multi-provider.md` | New | 1.51 |
| `docs/bip/1.52-catalog-ui-screenshots-openapi.md` | New | 1.52 |
| `docs/bip/1.53-dual-mode-auth-gateway.md` | New (from 1.58 content, corrected frontmatter) | 1.53 |
| `docs/bip/1.54-redis-rate-limiting.md` | New (from 1.59 content, corrected frontmatter) | 1.54 |
| `docs/execution-checklist.md` | Modify — mark `[x]` | 1.51, 1.52, 1.53, 1.54 |

---

## Checklist Items to Mark Done

After all work is implemented and verified:

- Change `[ ]` to `[x]` in `docs/execution-checklist.md` for tasks: **1.51**, **1.52**, **1.53**, **1.54**
